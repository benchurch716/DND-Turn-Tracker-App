<table class="table table-dark table-striped w-50 p-3">
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Effect</th>
        </tr>
    </thead>
    <tbody>
        {{#each conditions}}
        <tr>
            <td>{{name}}</td>
            <td>{{effect}}</td>
             <td>
                <form method="post">
                    <input type="hidden" name="charID" value="{{conID}}">
                    <input type="submit" name="Delete" value="Delete">
                </form>
            </td>
            <td>
                <form method="post">
                    <input type="hidden" name="charID" value="{{conID}}">
                    <input type="submit" name="Edit" value="Edit">
                </form>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
<h1>Add a new Condition:</h1>
<form class="row">
    <div class="mb-3 col-4">
        <label class="form-label">Name</label>
        <input type="text" class="form-control" id="name">
    </div>

    <div class="mb-3 col-4">
        <label class="form-label">Effect (Optional)</label>
        <input type="text" class="form-control" id="effect">
    </div>

    <div class="col">
        <button type="submit" class="btn btn-primary btn-lg col-3">Submit</button>
    </div>
</form>